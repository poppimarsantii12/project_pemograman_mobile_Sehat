package com.example.sehat;

import android.os.Bundle;
import androidx.appcompat.app.AppCompatActivity;

public class ProgressTrackerActivity extends AppCompatActivity {

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_progress_tracker);

        // Di sini Anda bisa menambahkan logika untuk mengisi data grafik
        // menggunakan library seperti MPAndroidChart atau sejenisnya.
    }
}